[project]
name = "goblin-recon"
version = "0.1.0"
description = "Hermes agent assets for AI trend research and content briefing."
requires-python = ">=3.12"
dependencies = [
  "youtube-transcript-api==1.2.4",
]

[tool.uv]
package = false

[dependency-groups]
dev = [
  "pytest==8.3.4",
]
