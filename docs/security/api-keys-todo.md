# API Keys To Review Tomorrow

No keys should be pasted into this file. Store real values only in local `.env` or your approved secret manager.

## Optional Discovery Integrations

| Key | Purpose | Status |
|---|---|---|
| `BRAVE_API_KEY` | Optional search MCP if free-tier search is needed. | Not configured |
| `FIRECRAWL_API_KEY` | Optional public web extraction fallback. | Not configured |
| `TAVILY_API_KEY` | Optional future search provider. | Not configured |
| `APIFY_TOKEN` | Optional future actor-based scraping provider. | Not configured |

Keep Tavily and Apify as placeholders only until the team approves the exact MCP server, allowed sources, budget, and compliance limits.

## Optional Platform APIs

| Key | Purpose | Status |
|---|---|---|
| `YOUTUBE_API_KEY` | Optional metadata API; not required for current yt-dlp path. | Not configured |
| `REDDIT_CLIENT_ID` | Optional Reddit API access. | Not configured |
| `REDDIT_CLIENT_SECRET` | Optional Reddit API access. | Not configured |
| `X_BEARER_TOKEN` | Optional X/Twitter public search. | Not configured |
| `NOTION_API_KEY` | Optional approved content tracker. | Not configured |
